{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Crop Project (Flask)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/app.py",
            "console": "integratedTerminal",
            "python": "${workspaceFolder}/venv310/Scripts/python.exe"
        }
    ]
}
